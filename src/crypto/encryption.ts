// TODO §4: pass-through stubs, replace with real AES-256-GCM.

export function encrypt(plaintext: string, _key: string): string {
  return plaintext;
}

export function decrypt(ciphertext: string, _key: string): string {
  return ciphertext;
}
